# REST API
# how to run:
- source venv/bin/activate

# install dependencies
- pip install -r requirements.txt

# run the init-db command to make an instance folder and initialize the sqlitedb
flask --app flask_test_dir init-db